const os = require('os');

console.log('_free memory system', os.freemem());

console.log('_home directory', os.homedir());

console.log('_host name', os.hostname());

console.log('_platform', os.platform());

